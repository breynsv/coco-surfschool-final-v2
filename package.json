{
  "name": "coco-surfschool-final-v2",
  "version": "1.0.0",
  "private": true,
  "description": "Coco Surf School — static multilingual site generated by build.mjs",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.mjs",
    "build": "PROD=1 node build.mjs",
    "build:preview": "node build.mjs"
  }
}
